#!/usr/bin/env ruby

dir = File.expand_path("..", __FILE__)

javaopts = ENV['JAVA_OPTS'] || ""

args = " -jar #{dir}/../target/scala-2.10/sprack.jar "

command = "java " << javaopts << args

ARGV.each{|a| command << " #{a} "  }

Kernel.exec(command)
